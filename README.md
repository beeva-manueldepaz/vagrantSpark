Instrucciones
=============

Ejecutar:

```bash
> vagrant up && vagrant ssh
> spark-shell
```

Listo para probar

Para MAC y Linux
----------------

Asegurarse de que el fichero `install.sh` tiene permisos de ejecucin:

```bash
> chmod +x install.sh
```
